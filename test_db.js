const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const f = await prisma.formaPago.findMany(); console.log(JSON.stringify(f, null, 2)); if (!f.some(x => x.tipo === 'por_cobrar')) { await prisma.formaPago.create({ data: { nombre: 'Por cobrar', tipo: 'por_cobrar', descripcion: 'Venta por cobrar', activa: true, usuarioCreacion: 'system', usuarioModificacion: 'system' } }); console.log('created por_cobrar'); } } main().catch(console.error).finally(() => prisma.$disconnect());
